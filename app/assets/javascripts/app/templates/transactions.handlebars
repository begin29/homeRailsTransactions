<h1>Transactions...</h1>
{{#each transaction in controller}}
  {{render "transaction" transaction}}
{{/each}}

{{#if isNewVisible}}
  {{view App.NewTransactionView}}
{{/if}}

<div class='actions'>
  <a href="#" {{action "showNew"}}>Add Transaction</a>
</div>